<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.ar" android:required="true"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"

        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyHipicAppTFG">

        <!-- ✅ meta-data DENTRO de application, no fuera -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="required"/>

        <activity
            android:name=".ui.MenuFuncionalidadesActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ✅ Activities que faltaban declaradas -->
        <activity
            android:name=".ui.QRScannerActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.ARMenuActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.RutasMenuActivity"
            android:exported="false"/>


        <activity
            android:name=".ui.ARActivity"
            android:exported="false"/>

        <!-- Rutas -->


        <activity
            android:name=".ui.GrabarRutaActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.ListaRutasActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.SeguirRutaActivity"
            android:exported="false"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAJbz6aWuCMnZz2or0e78jC6-ORrC0s8UE" />

    </application>

</manifest>