<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FAF8F5"> <!-- Fondo M3 Surface: Tono crema/arena muy claro y limpio -->

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbarAlumnos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="?attr/actionBarSize"
        android:background="#5D3A1A"
        app:navigationIcon="?attr/homeAsUpIndicator"
        app:navigationIconTint="#FFFFFF">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="8dp"
            android:paddingEnd="16dp"
            android:paddingVertical="8dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Alumnos inscritos"
                android:textColor="#EAD9C9"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium"
                android:letterSpacing="0.05" />

            <TextView
                android:id="@+id/tv_titulo_alumnos"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Doma · Intermedio"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:fontFamily="sans-serif-medium" />

        </LinearLayout>

    </com.google.android.material.appbar.MaterialToolbar>

    <!-- CONTADOR: Resaltado sutilmente con el acento Dorado Cuero (#C8860A) -->
    <TextView
        android:id="@+id/tv_contador_alumnos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="0 alumnos inscritos"
        android:textColor="#C8860A"
        android:textSize="14sp"
        android:fontFamily="sans-serif-medium"
        android:paddingHorizontal="24dp"
        android:paddingTop="20dp"
        android:paddingBottom="8dp" />

    <!-- ESTADO VACÍO: Estilizado para que no parezca un error, sino una pantalla limpia -->
    <TextView
        android:id="@+id/tv_sin_alumnos"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="Ningún alumno inscrito en esta clase"
        android:textColor="#8A7B6E"
        android:textSize="16sp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:paddingHorizontal="32dp"
        android:visibility="gone" />

    <!-- LISTA DE ALUMNOS (RECYCLERVIEW) -->
    <!-- Nota: Asegúrate de que el layout individual de las filas del RecyclerView (el ítem del alumno) -->
    <!-- use esquinas redondeadas (CardView de 12dp a 16dp) y fondos en #FFFFFF para el contraste M3. -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_alumnos_clase"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp" />

</LinearLayout>